using Bank.Api.ViewModels;
using System.Security.Claims;

namespace Bank.Api.Core
{
    public class Helper
    {
        public static ClaimViewModel GetUserClaim(ClaimsPrincipal user)
        {
            // All the code the extract to token and get the unique user id
            // Do here 


            return new();
        }
    }
}
